export const renderProgramsPage = ({ content, cmsMode }) => `<main id="main-content">
  <section class="section"><div class="container"><header class="section-header"><h1>Programs</h1></header></div></section>
  <section class="section"><div class="container"><header class="section-header"><h2>Featured programs</h2></header><div class="section-body grid three-up">${content.programs.map((program, index) => `<article class="card"><h3 contenteditable="${cmsMode}" data-edit-type="program" data-index="${index}" data-field="name">${program.name}</h3><p class="muted">${program.audience} · ${program.level}</p><p contenteditable="${cmsMode}" data-edit-type="program" data-index="${index}" data-field="description">${program.description}</p><p class="muted" contenteditable="${cmsMode}" data-edit-type="program" data-index="${index}" data-field="schedule">${program.schedule}</p></article>`).join('')}</div></div></section>
  ${cmsMode ? '<section class="section"><div class="container"><div class="section-body"><button class="btn" data-action="save-programs">Save Program Changes</button></div></div></section>' : ''}
</main>`;
