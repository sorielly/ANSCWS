export const renderProgramsPage = ({ content, cmsMode }) => `<main id="main-content"><section><h1>Programs</h1></section><section class="grid three-up">${content.programs.map((program, index) => `<article class="card"><h3 contenteditable="${cmsMode}" data-edit-type="program" data-index="${index}" data-field="name">${program.name}</h3><p class="muted">${program.audience} · ${program.level}</p><p contenteditable="${cmsMode}" data-edit-type="program" data-index="${index}" data-field="description">${program.description}</p><p class="muted" contenteditable="${cmsMode}" data-edit-type="program" data-index="${index}" data-field="schedule">${program.schedule}</p></article>`).join('')}</section>${cmsMode ? '<button class="btn" data-action="save-programs">Save Program Changes</button>' : ''}</main>`;
