export const renderAboutPage = ({ content, cmsMode }) => `<main id="main-content"><section><p class="eyebrow text-subtle">Club Story</p><h1>About</h1></section>
<section class="card"><h2>Mission</h2><p class="content-measure" contenteditable="${cmsMode}" data-edit-type="about" data-field="mission">${content.about.mission}</p><h2>History</h2><p class="content-measure" contenteditable="${cmsMode}" data-edit-type="about" data-field="history">${content.about.history}</p>${cmsMode ? '<button class="btn" data-action="save-about">Save About Changes</button>' : ''}</section>
<section class="grid two-up"><article class="card"><h2>Executive Team</h2>${content.about.executives.map((leader) => `<p><span class="text-strong">${leader.name}</span><br><span class="text-muted">${leader.role}</span></p>`).join('')}</article><article class="card"><h2>FAQ</h2>${content.about.faqs.map((faq) => `<p class="content-measure"><span class="text-strong">${faq.q}</span><br>${faq.a}</p>`).join('')}</article></section>
</main>`;
