export const renderAboutPage = ({ content, cmsMode }) => `<main id="main-content">
  <section class="section"><div class="container"><header class="section-header"><h1>About</h1></header></div></section>
  <section class="section"><div class="container"><header class="section-header"><h2>Featured story</h2></header><div class="section-body"><article class="card"><h3>Mission</h3><p contenteditable="${cmsMode}" data-edit-type="about" data-field="mission">${content.about.mission}</p><h3>History</h3><p contenteditable="${cmsMode}" data-edit-type="about" data-field="history">${content.about.history}</p>${cmsMode ? '<button class="btn" data-action="save-about">Save About Changes</button>' : ''}</article></div></div></section>
  <section class="section"><div class="container"><header class="section-header"><h2>Supporting details</h2></header><div class="section-body grid two-up"><article class="card"><h3>Executive Team</h3>${content.about.executives.map((leader) => `<p><strong>${leader.name}</strong><br><span class="muted">${leader.role}</span></p>`).join('')}</article><article class="card"><h3>FAQ</h3>${content.about.faqs.map((faq) => `<p><strong>${faq.q}</strong><br>${faq.a}</p>`).join('')}</article></div></div></section>
</main>`;
