export const renderRentalsPage = ({ content, cmsMode }) => `<main id="main-content"><section><p class="eyebrow text-subtle">Equipment</p><h1>Rentals</h1></section><section class="grid three-up">${content.rentals.map((rental, index) => `<article class="card"><h3 contenteditable="${cmsMode}" data-edit-type="rental" data-index="${index}" data-field="name">${rental.name}</h3><p class="key-number" contenteditable="${cmsMode}" data-edit-type="rental" data-index="${index}" data-field="price">${rental.price}</p><p class="content-measure" contenteditable="${cmsMode}" data-edit-type="rental" data-index="${index}" data-field="includes">${rental.includes}</p><p class="text-muted" contenteditable="${cmsMode}" data-edit-type="rental" data-index="${index}" data-field="notes">${rental.notes}</p></article>`).join('')}</section>${cmsMode ? '<button class="btn" data-action="save-rentals">Save Rental Changes</button>' : ''}</main>`;
