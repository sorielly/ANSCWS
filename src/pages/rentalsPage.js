export const renderRentalsPage = ({ content, cmsMode }) => `<main id="main-content">
  <section class="section"><div class="container"><header class="section-header"><h1>Rentals</h1></header></div></section>
  <section class="section"><div class="container"><header class="section-header"><h2>Featured rental options</h2></header><div class="section-body grid three-up">${content.rentals.map((rental, index) => `<article class="card"><h3 contenteditable="${cmsMode}" data-edit-type="rental" data-index="${index}" data-field="name">${rental.name}</h3><p><strong contenteditable="${cmsMode}" data-edit-type="rental" data-index="${index}" data-field="price">${rental.price}</strong></p><p contenteditable="${cmsMode}" data-edit-type="rental" data-index="${index}" data-field="includes">${rental.includes}</p><p class="muted" contenteditable="${cmsMode}" data-edit-type="rental" data-index="${index}" data-field="notes">${rental.notes}</p></article>`).join('')}</div></div></section>
  ${cmsMode ? '<section class="section"><div class="container"><div class="section-body"><button class="btn" data-action="save-rentals">Save Rental Changes</button></div></div></section>' : ''}
</main>`;
